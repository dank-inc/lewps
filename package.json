{
  "name": "@dank-inc/lewps",
  "version": "0.4.0",
  "description": "Some dank loop functions!",
  "private": false,
  "files": [
    "dist"
  ],
  "main": "dist/index.js",
  "scripts": {
    "play": "ts-node src/playground.ts",
    "lint": "tsc --noEmit",
    "compile": "rm -rf dist && tsc",
    "hitit": "npm run compile && npm publish",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Elijah Lucian",
  "repository": {
    "url": "https://github.com/dank-inc/lewps"
  },
  "license": "ISC",
  "devDependencies": {
    "ts-node": "^9.1.1",
    "typescript": "^4.2.3"
  }
}
